root node BinOp {
    children {
        Num left {constructor},
        Num right {constructor}
    }
};

node Num {
    attributes {
        int value { constructor }
    }
};

start phase RootPhase {
    prefix = RP,    
    actions {
        CreateAST;
        traversal Print; 
    }
};

pass CreateAST;
