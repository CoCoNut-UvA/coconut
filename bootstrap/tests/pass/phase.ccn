
start phase hello {
    actions {
        pass p;
        phase1;
    }
};

root node ast {
    children {
        ast next;
    }
};

node tmp {
    children {
        ast nextt;
    }
    attributes {
        int val;
        ast ref;
    }
};

nodeset x = {ast} | {ast};
nodeset y = x | {ast};
nodeset z = y | { tmp };
nodeset b = z & {tmp} & y;

traversal all {
    prefix = a
    
    travdata = {
        int fill
    }
};

traversal help {
    prefix = h

    nodes = x
    
    travdata = {
        int fill
    }
};


phase phase1 {
    actions {
        pass doSomething;
    } 
};

enum binopvalues {
    prefix = BO

    values = {
        add, sub
    }
}
