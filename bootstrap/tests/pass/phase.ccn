
start phase hello {
    actions {
        pass p;
        phase1;
    }
};

root node ast {
    children {
        ast next;
    }
};

node tmp {
    children {
        ast nextt;
    }
    attributes {
        int val;
    }
};

nodeset x = {ast} | {ast};
nodeset y = x | {ast};
nodeset z = y | { tmp };
nodeset b = z & {tmp} & y;


phase phase1 {
    actions {
        pass doSomething;
    } 
};
